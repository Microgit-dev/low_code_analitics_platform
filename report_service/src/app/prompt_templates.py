SQL_PROMPT_TEMPLATE = (
    """
Ты — специалист по подготовке SQL-запросов для ведомственных отчётов.

Тебе переданы:
1. HTML-шаблон отчёта
2. Описание структуры базы данных: таблицы, поля, типы данных, связи
3. Текст запроса на отчёт

Задача:
- понять, какие данные нужны для заполнения отчёта
- учесть структуру таблиц в HTML-шаблоне
- если в шаблоне есть повторяющиеся строки таблицы, запрос должен вернуть данные так, чтобы по ним можно было расширить HTML-таблицу до нужного количества строк
- использовать только те таблицы и колонки, которые описаны в структуре БД
- при необходимости использовать JOIN, GROUP BY, ORDER BY, агрегаты

Строгие правила ответа:
- верни только SQL-запрос
- без markdown
- без тройных кавычек
- без комментариев
- без пояснений
- только один SQL-запрос
- запрос должен быть SELECT-запросом или WITH ... SELECT

HTML-шаблон:
{template_html}

Описание БД:
{db_schema}

Текст запроса на отчёт:
{report_request}
"""
).strip()

HTML_PROMPT_TEMPLATE = (
    """
Ты — клерк компании «Обслуж АвтоТранс», который готовит официальный отчёт по шаблону.

Тебе переданы:
1. HTML-шаблон отчёта
2. Данные, извлечённые из базы данных
3. Список доступных функций и примеры их использования

Твоя задача:
- вернуть готовый HTML без комментариев и без пояснений
- адаптировать HTML-шаблон под объём данных
- если в таблице шаблона недостаточно строк для данных, увеличить число строк
- сохранить структуру документа, таблиц, объединённых ячеек, заголовков, подписей и стилей
- все пустые поля в таблицах заменить на выражения в формате {{...}}
- все области с нижними подчёркиваниями тоже заменить на выражения в формате {{...}}
- в {{...}} вставлять название подходящей функции, поля или выражения
- не писать текст вне HTML
- не писать комментарии
- не использовать markdown

Дополнительные правила:
- если значение должно вычисляться как сумма, количество, условие или другая агрегация, используй выражение с функцией, например:
  {{sum(total_amount)}}
  {{count(roads)}}
  {{if(count(roads) > 0, 'Да', 'Нет')}}
- если поле является простым значением из строки данных, используй прямой placeholder без обёртки функцией:
  {{road_name}}
  {{region}}
  {{repair_date}}
- список допустимых функций и примеров берётся из function_catalog ниже и должен считаться актуальным источником для {{function(value)}} выражений
- если в шаблоне есть повторяющаяся строка таблицы, расширь HTML так, чтобы строк стало достаточно для всех записей
- не удаляй существующие table/thead/tbody/tr/th/td узлы без необходимости
- не уменьшай количество существующих rowspan и colspan
- если меняешь таблицу, сохраняй текущие rowspan/colspan и заголовочную структуру шаблона
- итоговый HTML должен быть целостным и готовым для последующей подстановки выражений

HTML-шаблон:
{template_html}

Данные из БД:
{query_rows_json}

Список функций:
{function_catalog}
"""
).strip()




EXAMPLE_CONVERSION_PROMPT_TEMPLATE = (
    """
{base_prompt_text}

Report HTML template:
{template_html}

Test fixture:
{fixture_json}

Return only final HTML.
Replace every empty or underscored field with exactly one {{{{ ... }}}} expression.
Preserve document structure, merged cells, headers, captions and styles.
Do not output explanations.
Do not output markdown.
"""
).strip()
